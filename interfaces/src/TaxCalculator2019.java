// TaxCalculator2019 is an implementation of the TaxCalculator interface
// This version is intended to calculate tax based on 2019's rules.
// Currently, it returns 0 as a placeholder and can be updated with actual tax logic later.
public class TaxCalculator2019 implements TaxCalculator {

    /**
     * Calculates the tax based on 2019 tax rules.
     * Currently returns 0 — this is a stub/placeholder implementation.
     *
     * @return Calculated tax (currently hardcoded as 0).
     */
    @Override
    public double calculateTax() {
        return 0;
    }
}
